{
  "name": "the-novel",
  "version": "0.0.0",
  "private": true,
  "description": "A novel written chapter by chapter. This package exists only to run the repository's own checks  there is no application here.",
  "scripts": {
    "check": "node tools/check-chapters.mjs",
    "test": "node tools/check-chapters.mjs tools/fixtures --expect-fail",
    "check:report": "node tools/check-chapters.mjs --report=tools/validation/REPORT.json",
    "toc": "node tools/build-toc.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
